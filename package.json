{
  "name": "setlog",
  "version": "1.0.0",
  "private": true,
  "description": "Personal workout tracker: sets, weights, rest timer, progress.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "engines": {
    "node": ">=18"
  }
}
